# Docker Compose configuration for pyiwfm web visualization
#
# Usage:
#   docker-compose up --build
#
# Access the web viewer at http://localhost:8080

services:
  viewer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pyiwfm-viewer
    ports:
      - "8080:8080"
    volumes:
      # Mount your IWFM model directory (update path as needed)
      - /path/to/your/model:/model:ro
      # Mount output directory for exports
      - ./output:/output
    environment:
      # Web server configuration
      - PORT=8080
      - TITLE=IWFM Sample Model Viewer
    # Allow container to run Xvfb
    privileged: false
    # Resource limits (adjust based on model size)
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 1G
    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # Restart policy
    restart: unless-stopped

  # Full image with HEC-DSS support (compiled from source)
  viewer-dss:
    build:
      context: .
      dockerfile: Dockerfile.full
    container_name: pyiwfm-viewer-dss
    profiles:
      - dss  # Only start with: docker-compose --profile dss up viewer-dss
    ports:
      - "8080:8080"
    volumes:
      - /path/to/your/model:/model:ro
      - ./output:/output
    environment:
      - PORT=8080
      - TITLE=IWFM Sample Model Viewer
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 1G
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped

  # Alternative: Interactive Python environment for development
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pyiwfm-dev
    profiles:
      - dev  # Only start with: docker-compose --profile dev up
    ports:
      - "8888:8888"  # Jupyter
      - "8081:8080"  # Web viewer on different port
    volumes:
      - /path/to/your/model:/model:ro
      - ./src:/app/src:ro  # Mount source for development
    environment:
      - PORT=8080
      - DISPLAY=:99
      - PYVISTA_OFF_SCREEN=true
    command: >
      bash -c "
        Xvfb :99 -screen 0 1024x768x24 &
        pip install jupyter &&
        jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser --allow-root
      "
    stdin_open: true
    tty: true
